package com.example.battleship.const

import androidx.compose.foundation.layout.PaddingValues

object Mode {
    var isGestureNav = false
    var isThreeNav = false
    var paddingValues = PaddingValues()
}